we are borrowing a lot of code from ../tcc-ge-dcs look there for help/inspiration
we are intentionally rethinking the backend to remove dcs from the loop except for once daily
new worktree? run `scripts/worktree-init.ps1` from the worktree root to junction node_modules from main (skips `npm install`)